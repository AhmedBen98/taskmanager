{% extends 'base.html.twig' %}

{% block title %}Modifier la tâche{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/starability/starability-all.min.css">
{% endblock %}

{% block body %}
<div class="container mt-5">
    <div class="card shadow border-0">
        <div class="card-header fw-bold text-white" style="background: linear-gradient(90deg, #f73587, #ffa726);">
            ✏️ Modifier la tâche
        </div>
        <div class="card-body">
            {{ form_start(form) }}
                <div class="mb-3">
                    {{ form_label(form.title, 'Titre') }}
                    {{ form_widget(form.title, { 'attr': {'class': 'form-control'} }) }}
                </div>

                <div class="mb-3">
                    {{ form_label(form.description, 'Description') }}
                    {{ form_widget(form.description, { 'attr': {'class': 'form-control'} }) }}
                </div>

                <div class="mb-3 form-check">
                    {{ form_widget(form.isDone, { 'attr': {'class': 'form-check-input'} }) }}
                    {{ form_label(form.isDone, 'Terminée', { 'label_attr': {'class': 'form-check-label'} }) }}
                </div>

                <div class="mb-3">
                    {{ form_label(form.createdAt, 'Date de création') }}
                    {{ form_widget(form.createdAt, { 'attr': {'class': 'form-control'} }) }}
                </div>

                <div class="mb-4">
                    {{ form_label(form.difficulty, 'Difficulté') }}
                    <div class="starability-basic">
                        {{ form_widget(form.difficulty) }}
                    </div>
                </div>

                <button class="btn text-white" style="background: linear-gradient(to right, #f73587, #ffa726);">
                    💾 Enregistrer
                </button>
            {{ form_end(form) }}

            <div class="d-flex justify-content-between align-items-center mt-4">
                <a href="{{ path('app_task_index') }}" class="btn btn-outline-dark">
                    ← Retour à la liste
                </a>

                {{ include('task/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
</div>
{% endblock %}
