{% extends 'base.html.twig' %}

{% block title %}Liste des tâches{% endblock %}

{% block stylesheets %}
    <link rel="stylesheet" href="https://cdn.datatables.net/1.13.6/css/dataTables.bootstrap5.min.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/starability/starability-all.min.css">
{% endblock %}

{% block body %}
<div class="container mt-5">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="fw-bold" style="color: #f73587;">📋 Mes Tâches</h1>
        <a href="{{ path('app_task_new') }}" class="btn text-white" style="background-color: #f73587;">
            ➕ Nouvelle tâche
        </a>
    </div>

    <div class="d-flex justify-content-between mb-3">
        <div>
            <strong>Total :</strong> {{ tasks|length }} tâche(s)
        </div>
    </div>

    <div class="card shadow-sm border-0">
        <div class="card-body p-4">
            <table id="tasksTable" class="table table-striped table-bordered align-middle text-center">
                <thead style="background: linear-gradient(to right, #f73587, #ffa726); color: white;">
                    <tr>
                        <th>Id</th>
                        <th>Titre</th>
                        <th>Description</th>
                        <th>Terminée</th>
                        <th>Difficulté</th>
                        <th>Date création</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                {% for task in tasks %}
                    <tr>
                        <td>{{ task.id }}</td>
                        <td class="task-title">{{ task.title }}</td>
                        <td>{{ task.description|length > 30 ? task.description[:30] ~ '...' : task.description }}</td>
                        <td>
                            <input type="checkbox" {% if task.isDone %}checked{% endif %} disabled>
                        </td>
                        <td>
                            {% for i in 1..5 %}
                                {% if i <= task.difficulty %}<span style="color:#f73587">★</span>{% else %}<span style="color:#ccc">☆</span>{% endif %}
                            {% endfor %}
                        </td>
                        <td>{{ task.createdAt ? task.createdAt|date('Y-m-d H:i') : '—' }}</td>
                        <td>
                            <a href="{{ path('app_task_show', {'id': task.id}) }}" class="btn btn-sm" style="color: #f73587; border: 1px solid #f73587;">🔍 Voir</a>
                            <a href="{{ path('app_task_edit', {'id': task.id}) }}" class="btn btn-sm" style="color: #ffa726; border: 1px solid #ffa726;">✏️ Modifier</a>
                        </td>
                    </tr>
                {% else %}
                    <tr>
                        <td colspan="7" class="text-muted text-center">Aucune tâche enregistrée.</td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
</div>
{% endblock %}

{% block javascripts %}
    <script src="https://cdn.datatables.net/1.13.6/js/jquery.dataTables.min.js"></script>
    <script src="https://cdn.datatables.net/1.13.6/js/dataTables.bootstrap5.min.js"></script>
    <script>
        document.addEventListener("DOMContentLoaded", function () {
            new DataTable('#tasksTable', {
                language: {
                    url: '//cdn.datatables.net/plug-ins/1.13.6/i18n/fr-FR.json'
                }
            });
        });
    </script>
{% endblock %}
